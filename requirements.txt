# Core TTS
qwen-tts

# Audio I/O
soundfile

# Tensor / ML
# NOTE: This installs the CPU-only build of PyTorch by default.
# For GPU (CUDA) support, install manually with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   (adjust cu118 to match your CUDA version: cu121, cu124, etc.)
torch
torchvision
torchaudio

# Web UI
gradio

# Utilities
numpy
python-dotenv

# HuggingFace fast transfer
hf_xet