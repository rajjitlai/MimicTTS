# ─────────────────────────────────────────────────────────────
# MimicTTS — Environment Configuration
# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   Windows:  copy .env.example .env
#   Linux:    cp .env.example .env
# ─────────────────────────────────────────────────────────────


# ── Model ─────────────────────────────────────────────────────
# Options:
#   Qwen/Qwen3-TTS-12Hz-0.6B-Base   (lighter, faster)
#   Qwen/Qwen3-TTS-12Hz-1.7B-Base   (better quality)
MODEL_ID=Qwen/Qwen3-TTS-12Hz-0.6B-Base


# ── Device ────────────────────────────────────────────────────
# Options: cuda:0 | cuda:1 | cpu
# Leave blank to let the app auto-detect (recommended)
DEVICE=cuda:0


# ── Paths ─────────────────────────────────────────────────────
REFERENCE_AUDIO_DIR=reference_audio
OUTPUT_DIR=outputs


# ── Defaults ──────────────────────────────────────────────────
DEFAULT_LANGUAGE=English
DEFAULT_OUTPUT_FILE=outputs/result.wav


# ── Gradio ────────────────────────────────────────────────────
# Set to "true" to make the UI accessible on your local network
# Useful if running on a remote machine or WSL
GRADIO_SHARE=false

# Port for the Gradio web UI (default: 7860)
GRADIO_PORT=7860


# ── HuggingFace ───────────────────────────────────────────────
# Optional: your HF token if you need access to gated models
# Get yours at https://huggingface.co/settings/tokens
HF_TOKEN=